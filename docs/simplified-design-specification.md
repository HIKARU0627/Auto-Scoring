# AI一次添削支援アプリ 簡易設計書

## 1. 概要

### 1.1 目的

塾から提供される以下の資料を利用し、答案添削作業を効率化する。

必須:

- 生徒答案（PDF）
- 採点基準（PDF）

任意（あれば精度が上がる）:

- 添削資料（Word / Excel）
- 添削サンプル（PDF）

**PDFだけが来る前提を置かない。** 実際の配布物にはWord / Excelが含まれる。
詳細は §4、実資料で確認できた構造は
[`grading-material-structure.md`](./grading-material-structure.md)。

AIが一次添削を行い、人間はAIの結果を確認・修正・承認することを基本とする。

完全自動採点ではなく、**Human-in-the-loop型の添削支援システム**として設計する。

効率的に採点を行うために並列で採点を行えるようにする。

---

## 2. システムの基本方針

### 2.1 AIの役割

AIは以下を担当する。

- 採点資料（採点基準・添削資料）の役割判定と構造抽出（§4.3）
- 生徒答案の読み取り
- 採点基準に基づく判定
- 点数候補の生成
- ○・×等の添削記号候補の生成
- コメント候補の生成
- 判定根拠の生成
- 判定信頼度の算出

AIの判定を最終結果とはしない。

### 2.2 人間の役割

人間は以下を担当する。

- AIによる文字認識結果の確認
- AI採点結果の確認
- 添削位置の確認
- 点数・コメントの修正
- AI結果の承認または却下
- 判読困難な答案の手動添削

---

# 3. 対象環境

## 3.1 アプリ形式

Windows向けデスクトップアプリを基本とする。

### 暫定技術

- Flutter
- Dart
- Material Design 3

### 対応プラットフォーム

- Windows以外も対応するか
  - macOS、Linuxへは将来的に対応させる

---

# 4. 入力データ

> **本章はIssue #95で全面的に書き換えた。** 旧版は「模範解答PDF」と「採点マニュアルPDF」を
> 必須入力としていたが、実際の採点者が持っている資料と対応していなかった。
> 実資料で確認できた構造は
> [`grading-material-structure.md`](./grading-material-structure.md) に記録している。

## 4.1 必須入力

1つのテストについて、必須は次の2つだけとする。

### 採点基準PDF

**このテストの採点に必要な情報が入った資料。** 実資料では、1本のPDFに

- 問題（設問文）
- 模範解答
- 解説
- 配点・採点基準

がまとめて入っていた（[実資料の構造](./grading-material-structure.md) §3.3）。
**ただしこの4点が常に揃っていることを前提にしない。** 取り出せた項目だけを使い、
取り出せなかった項目は「取れなかった」として人に確認させる（§4.4）。

### 生徒答案PDF

実際に添削する対象。

---

## 4.2 任意入力

### 添削資料（任意だが強く推奨）

誤答パターンと、それに対する赤入れ（コメント）の文例。**Word / Excel で配布される。**

- **無くても登録・採点できる。** ただしコメント候補の質が落ちる
- **無い場合は画面で警告し、精度が落ちることを明示する**（決定3）
- 形式は Word / Excel のどちらもありうる。実資料では教科によって、同じ役割の情報が
  表（Excel）だったり地の文（Word）だったりした

### 添削サンプル（任意）

添削済み答案の見本。採点形式やコメントの入れ方の参考として登録できる。

### 模範解答PDF（廃止）

**必須入力から外した**（決定1）。理由は次の2つ。

1. **採点基準PDFが模範解答を既に含んでいる。** 別に要求する理由がない
2. **「回答欄に模範解答が書かれた形式のPDF」を採点者が持っているとは限らない。**
   旧版はこれをテストプロファイルの自動解析の入力にしていたが、その前提が成り立たない

模範解答が別ファイルで手元にある場合は、参考資料として §4.2 の枠で追加してよい。
**アプリはそれを前提にしない。**

---

## 4.3 形式非依存の取り込み — 設計方針

> **この方針はIssue #95でオーナーが指示したものである。**
>
> > 今回のケースがこれからの採点資料と合致するとは限らないため、いろいろなケースに
> > 対応できるようにしてください。分類にAIやLLMを使用しても構いません

**形式ごとのparserを書かない。文書をLLMに理解させ、人が確認する。**

### 4.3.1 なぜparserを書かないか

実資料を調べた結果、**形式を仮定した実装はどこかで必ず外れる**ことが分かった
（[実資料の構造](./grading-material-structure.md)）。

- 採点基準PDFの機械可読性が3通りに割れる。**「抽出文字数 > 0」はOCR要否の判定に使えない**
  （縦書きの文書は、文字は取れるのに読み順が壊れる）
- 添削資料のExcelは、7本に対して**列スキーマが4種類**あり、**見出し行が1行目ですらない**
- 同じ役割の情報が、教科によって表だったり地の文だったりする
- ファイル名の規則が揃っておらず、名前から役割を判定できない

**これは1つの塾の1期分のサンプルでしかない。** 次の塾の資料が同じ形である保証はない。
形式ごとにparserを足していく方針は、資料が増えるたびに破綻する。

### 4.3.2 取り込みの4段階

```text
1. 機械的な下ごしらえ（形式非依存）
   PDF   → pdfiumでテキスト抽出 + 全ページを画像化
   Word  → テキストと表を素直に取り出す（構造は仮定しない）
   Excel → 全シートのセルを素直に取り出す（見出し行を仮定しない）
        ↓
2. 役割の判定（LLM）
   「この文書は答案か、採点基準か、参考資料か、添削サンプルか」を
   ファイル名ではなく中身から判定する
        ↓
3. 構造の抽出（LLM）
   設問・配点・採点の観点・よくある誤り・コメント例を、
   表でも地の文でも同じ出力スキーマに落とす
        ↓
4. 人による確認（必須の関門）
   抽出結果は提案であって確定ではない。
   取れなかった項目は「取れなかった」と表示する。推測で埋めない
```

段階1でテキストが得られなかった場合、および得られても読み順が壊れている疑いがある場合は、
**ページ画像を多モーダルモデルに渡す。** §8.3 も参照。

### 4.3.3 この方針の代償

**代償があることを承知のうえで採る方針である。** 隠さず記録しておく。

| 代償                                      | 内容                                                                       | どう扱うか                                                      |
| ----------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------- |
| 採点資料そのものを外部APIへ送ることになる | 塾の著作物であり、講師名・塾名を含む                                       | 決定7で許可済み。ただし**公開は禁止**（§26、業務ルール §2 (2)） |
| コストと待ち時間                          | ファイル1本ごとにLLM呼び出しが発生する。テスト登録が重くなる               | 登録は1テストにつき1回。答案ごとの採点とは頻度が違う            |
| **再現性が無い**                          | **同じ入力から同じ結果が出るとは限らない。抽出結果をテストで固定できない** | **だから人の確認が必須である**（§4.3.2 段階4）。§4.4 も参照     |

**3番目が最も重い。** 抽出の正しさをテストで担保できない以上、
**人の確認を省略可能な最適化として扱ってはならない。** 確認は仕様の一部である。

---

## 4.4 抽出に失敗したときの退避

**全項目を人が手入力できる画面を必ず用意する**（決定8）。

- LLMが役割判定・構造抽出のどちらに失敗しても、**登録作業が止まらないこと**
- 取れなかった項目は空欄として提示し、**推測値を埋めない**
- 一部だけ取れた場合は、取れた項目を初期値として提示し、取れなかった項目だけ人が埋める
- 「AIが抽出した値」と「人が入力・修正した値」を区別して保持する（§35-5）

詳細は §16.2・§24。

---

## 4.5 PDF構造

テストごとにPDFレイアウトが異なることを前提とする。

そのため、

> 全テスト共通の固定座標

は使用しない。

代わりに、テスト単位で「テストプロファイル」を生成する。

### 4.5.1 実資料で確認できた、設計が耐えるべきケース

以下は**仕様ではなく、実在が確認できた例**である
（[実資料の構造](./grading-material-structure.md)）。設計がこれらに耐えるか確認する材料として使う。

| ケース                                                                           | 根拠                                       |
| -------------------------------------------------------------------------------- | ------------------------------------------ |
| **設問とページの対応が多対多**（1設問が2ページに跨る／1ページに複数設問）        | 目視した3教科で3通りが同時に存在           |
| **縦書きの答案**（列が右から左）                                                 | 目視した3教科中1教科                       |
| 回答欄の形状が4通り（横書きマス目／縦書きマス目／小問1文字枠／罫線なし自由領域） | 同上                                       |
| **答案PDFが課題全体の一部**（フッタの総ページ数とPDFのページ数が一致しない）     | 目視した3教科中1教科で不一致               |
| **答案にQRコードが0〜2個**（個数が答案により違う）                               | 目視した3教科で0個・1個・2個がそれぞれ存在 |
| 採点基準PDFが完全な画像で抽出0文字                                               | 異なる10文書中6                            |

**「1ページ＝1設問」を既定値にしてはならない。** 旧版の設問依存DAG（§4章 業務ルール）は
複数ページ答案を前提にしており、この点は引き続き有効である。

---

# 5. テストプロファイル

## 5.1 概要

新しいテストを登録した際に、PDFの構造を解析し、そのテスト専用の情報を保存する。

例：

```text
テスト
 ├─ Page 1
 │   ├─ 問1
 │   ├─ 問2
 │   └─ 問3
 │
 └─ Page 2
     ├─ 問4
     └─ 問5
```

## 5.2 保存する情報

設問ごとに以下を保持する。

- 問題番号
- **ページ番号（複数可）** — 1設問が複数ページにまたがる（§4.5.1）
- 問題文領域
- 回答欄領域
- **書字方向**（横書き / 縦書き） — 縦書きの答案が実在する（§4.5.1）
- ○×等を配置する候補領域
- コメント配置候補領域
- 配点
- 採点ルール
- 模範解答（採点基準PDFから取れた場合。**無くてもよい**）

各項目は **「AIが抽出した値」か「人が入力・確定した値」かを区別して保持する**（§4.3.3・§35-5）。
**取れなかった項目は空のまま保持する。推測値で埋めない**（§4.4）。

座標については可能な限り0〜1の正規化座標で保持する。

> PoC 4（Issue #15）でこのスキーマの往復（生成→人間確認→再適用）を検証した。
> ドメインモデルは `RegionKind`（問題文領域／回答欄領域／○×等の候補領域／配点／
> 採点ルール／模範解答）ごとに `Region` を持ち、`Profile.status` が
> `DRAFT`（未確認）→`CONFIRMED`（確認済み）の一方向で遷移する。詳細は
> [`poc-4-multi-layout-profiles.md`](./poc-4-multi-layout-profiles.md)。

例：

```json
{
  "questionId": "q3",
  "page": 2,
  "answerArea": {
    "x": 0.15,
    "y": 0.42,
    "width": 0.7,
    "height": 0.18
  }
}
```

---

# 6. テスト登録フロー

```text
採点基準PDF（必須）
+ 生徒答案PDF（必須）
+ 添削資料 Word/Excel（任意・強く推奨）
+ 添削サンプルPDF（任意）
        ↓
形式非依存の下ごしらえ（§4.3.2 段階1）
        ↓
役割の判定・構造の抽出（LLM。§4.3.2 段階2-3）
   ├─ 採点基準から：設問・配点・採点の観点・模範解答
   └─ 答案から　　：回答欄・書字方向・設問とページの対応（§6.2）
        ↓
テストプロファイル生成（draft）
        ↓
人間による確認 ←→ 取れなかった項目は人が手入力（§4.4）
        ↓
必要なら領域・配点・採点基準を修正
        ↓
テスト登録完了（confirmed）
```

## 6.1 重要方針

AIによる推定を無条件で採用しない。

初回のみ人間が、

- 設問の対応
- 回答欄
- 配点
- 採点基準

を確認する。

**この確認は省略可能な最適化ではない。** §4.3.3 のとおり、LLMによる抽出は
**同じ入力から同じ結果が出ることを保証できず、テストで固定できない。**
人の確認が、正しさを担保する唯一の手段である。

## 6.2 回答欄は答案そのものから解析する

**決定2。** 回答欄の位置は、模範解答からではなく、**生徒答案のページ画像から**求める。

- 答案のページ画像を多モーダルモデルに渡し、**回答領域の候補**を出させる
- 併せて**書字方向**（横書き / 縦書き）と、**設問とページの対応**を推定させる
- 人が確認・修正して確定する

### なぜ答案からか

**教科ごとに様式が大きく変わり、共通の様式を仮定した自動解析が成立しないため。**
実資料では、回答欄の形状が4通り、設問とページの対応が3通り観測された（§4.5.1）。
そして旧版が入力に使おうとしていた「回答欄に模範解答が書かれたPDF」は、**そもそも存在しない**。

### 制約

- **1テストにつき、少なくとも1枚の答案が登録時に必要になる。** 答案が1枚も無い状態では
  回答欄を確定できない。テスト登録と答案取込の順序に影響する
- 答案ごとにスキャンのずれがあるため、確定した回答欄は**テスト単位のプロファイル**として持ち、
  答案ごとの位置合わせは §7 の前処理で吸収する

---

# 7. 答案解析

## 7.1 前処理

答案PDFをページ単位で画像化する。

必要に応じて以下を行う。

- 傾き補正
- 回転補正
- 拡大縮小補正
- ノイズ低減
- コントラスト調整
- テストプロファイルとの位置合わせ

画像処理にはOpenCVを使用する。

> 取込・保存・前処理の実装はIssue #17で行った。PDFの検証（拡張子・MIME・magic bytes・
> size上限・page数・暗号化・破損）、同一PDFの再取込方針、回答欄切り出しは
> 「テストプロファイルとの位置合わせ」ではなく`Question.answer_area`
> （Issue #11で導入済みの設問ごとの正規化座標）を直接使う実装とした。
> デスキュー等の前処理はプレビュー画像にのみ適用し、切り出し座標には影響しない。
> 詳細・未決事項は[`answer-intake-and-preprocessing.md`](./answer-intake-and-preprocessing.md)。

---

# 8. 手書き文字認識

## 8.1 基本方針

OCR結果のみを信用しない。

以下の2種類を組み合わせる。

```text
答案画像
   ├─ OCR
   │    ├─ 認識文字
   │    ├─ Bounding Box
   │    └─ Confidence
   │
   └─ Vision AI
```

Vision AIには可能であれば、

- 元答案画像
- OCR結果
- 模範解答
- 採点ルール

を同時に入力する。

---

## 8.2 Confidence

文字認識について信頼度を保持する。

例：

```text
HIGH
MEDIUM
LOW
```

または、

```text
0.00 ～ 1.00
```

### 低Confidenceの場合

自動で確定しない。

UI上で、

> 文字認識要確認

として人間へ提示する。

---

## 8.3 OCR技術

### OCR技術

以下のどれを使用するかは実際のパフォーマンステスト後に決定する。

- Vision対応LLM
- Google Document AI
- Azure AI Vision
- ローカルOCR
- 複数方式の併用

最初から固定しない。

`OCRProvider`として抽象化する。

---

# 9. AI採点

## 9.1 入力

設問ごとに以下をAIへ入力する。

- 問題文
- 生徒答案画像
- OCR結果
- 採点基準（採点の観点と、加点式 / 減点式の別を含む）
- 配点
- 模範解答（採点基準PDFから取れた場合。**必須ではない**）
- 添削資料から抽出した誤答パターン・コメント例（登録されている場合）

**採点方式は設問ごとに異なりうる。** 実資料では、観点ごとに積み上げる加点式と、
配点から引く減点式（「配点を超えて減点しない」等の下限規則を伴う）が教科間で混在していた
（[実資料の構造](./grading-material-structure.md) §3.5）。
業務ルール §2 (8) のとおり両方に対応する。

---

## 9.2 出力

AIから自由文だけを返させず、構造化データとして取得する。

例：

```json
{
  "questionId": "q3",

  "recognition": {
    "text": "光合成によって酸素が発生する",
    "confidence": 0.91
  },

  "grading": {
    "score": 4,
    "maxScore": 5,
    "confidence": 0.88
  },

  "criteria": [
    {
      "id": "criterion-1",
      "result": "pass",
      "confidence": 0.97
    },
    {
      "id": "criterion-2",
      "result": "partial",
      "confidence": 0.76
    }
  ],

  "comment": "理由の説明が不足しています。",

  "annotations": []
}
```

---

# 10. 採点Confidence

以下のConfidenceは分離する。

### Recognition Confidence

文字を正しく読み取れた可能性。

### Grading Confidence

読み取った答案に対して採点が正しい可能性。

例えば、

```text
文字認識：98%
採点判断：63%
```

という状態を許容する。

---

# 11. 添削Annotation

## 11.1 MVPで対応する種別（決定4）

**ボックス・下線・テキストに対応する。図形は将来対応とする。**

| 種別             | 用途                            |
| ---------------- | ------------------------------- |
| ボックス（囲み） | 対象箇所を囲む / 注釈を枠で囲む |
| 下線             | 対象箇所の指摘                  |
| テキスト         | コメント・点数・○×△等の記号     |

**テキストは書字方向を持つ。** 縦書きの答案には縦書きの注釈を置く（§4.5.1・§12）。
左横書きだけを想定した実装は、縦書きの教科で破綻する。

## 11.2 将来対応（MVP対象外）

**図形。** 特に**矢印**である。

## 11.3 実運用との差 — 既知の制約

**決定4の範囲は、実際の添削で使われている表現の一部でしかない。**
実資料の添削サンプルと、塾が配布している「定型オブジェクト集」で確認できたものは次のとおり
（[実資料の構造](./grading-material-structure.md) §6・§7.1）。

| 実際に使われているもの                                           | MVPでの扱い                                               |
| ---------------------------------------------------------------- | --------------------------------------------------------- |
| テキスト（縦書き含む）                                           | **対応**                                                  |
| 下線（直線・波線・破線）                                         | **対応**（線種の作り分けは未決）                          |
| 囲み（矩形・角丸の吹き出し）                                     | **対応**                                                  |
| ○ / × / △ / チェック相当                                         | **対応**（テキストとして描画）                            |
| **矢印**（直線・曲線・ループ・双方向）                           | **対象外。** 注釈と対象箇所を結ぶ主要な手段だった         |
| **括弧類**（`( )` `{ }` `[ ]` を答案に重ねて語のまとまりを示す） | **対象外**                                                |
| **手描きの図**（グラフ等）                                       | **対象外**                                                |
| インラインの加減点（`(+3)` `(-1)`）                              | **未決**（§11.4）                                         |
| 白い矩形（下地の罫線を隠す）                                     | **対象外**                                                |
| 他ページへの参照文                                               | **対象外**（注釈が1ページに収まらない場合に使われている） |

**特に重い制約**: ある教科では、添削の主体が**手描きのグラフ**だった。
**その教科はMVPの表現力では再現できない。** これは「まだ作っていない」ではなく
「MVPの範囲では代替できない」として認識すること。

### 矢印の代わりに何を使うか

矢印が使えない以上、**注釈と対象箇所の対応づけには参照記号を使う。**
実資料でも、小さな囲み記号（※1 等）を対象箇所と注釈ブロックの両方に置いて
対応づける方法が併用されていた。

**MVPはこの参照記号方式を前提に設計する。** ボックス・下線・テキストだけで
実運用に近づけるには、この仕組みが要る。

## 11.4 未決

- インラインの加減点表示（対象箇所ごとの `(+3)` `(-1)`）を扱うか。
  決定5は**設問ごと**の点数を扱うと定めており、部分ごとの加減点表示は含んでいない
- 下線の線種（直線・波線・破線）を作り分けるか

## 11.5 将来の拡張時に参照するもの

Annotation種別を拡張するときは、**推測で決めない。**
塾が配布している「定型オブジェクト集」（横書き版・縦書き版の2種）が、
**塾自身による「必要な添削表現」の答え**である
（[実資料の構造](./grading-material-structure.md) §7.1）。

---

# 12. Annotation位置決定

## 12.1 基本方針

AI自身にPDF座標を直接推測させない。

例えばAIは、

```json
{
  "target": "行く",
  "type": "correction",
  "comment": "過去形"
}
```

までを返す。

座標決定はアプリ側が担当する。

---

## 12.2 固定領域への記号

○・×・得点など、設問単位の記号については、

```text
Question
↓
テストプロファイル
↓
Annotation配置領域
```

から位置を求める。

---

## 12.3 特定文字への添削

OCRが取得したBounding Boxを利用する。

```text
AI
「この単語を訂正」

↓


OCR
対象単語のBounding Box

↓

Annotation位置決定
```

---

## 12.4 位置特定できない場合

無理に本文付近へ配置しない。

設問単位のコメント領域へ退避させる。

例：

```text
問3

⚠ 時制表現について確認
```

---

# 13. PDF表示

## 13.1 編集中

元PDF自体は直接編集しない。

```text
PDF
+
Annotation Overlay
```

として表示する。

### 利点

- ○×を移動可能
- コメントを編集可能
- AI添削を削除可能
- Undo可能
- PDF破損を防止

---

# 14. PDF出力

人間によるレビュー完了後、

```text
元PDF
+
確定Annotation
        ↓
pypdfium2 + pypdf
        ↓
添削済みPDF
```

> PDF ライブラリは PoC 3（Issue #12）で `pypdfium2` + `pypdf` に確定した。
> 経緯は [`technology-stack.md`](./technology-stack.md) §3.1 /
> [`poc-3-pdf-coordinates.md`](./poc-3-pdf-coordinates.md)。実装は Issue #23
> で行った。人間レビュー完了後の確定Annotationを○×△・下線・囲み・点数・
> コメントとして実際に描画し、`Export`（成功した出力）と`Job`
> （kind=`export`、出力の実行単位）として永続化する。日本語コメントの
> フォント埋め込みには`reportlab`を追加した。詳細・決定事項は
> [`pdf-export.md`](./pdf-export.md)。

として新しいPDFを生成する。

元PDFは変更しない。

## 14.1 点数の記載（決定5）

**添削済みPDFには、設問ごとの点数と合計点の両方を記載する。**

| 誰が         | 何を                           |
| ------------ | ------------------------------ |
| AIが提案する | **設問ごとの点数と、その根拠** |
| 人が確定する | **設問ごとの点数**             |
| PDFに載る    | **設問ごとの点数 ＋ 合計点**   |

合計点は、確定した設問ごとの点数から**アプリが算出する**。
AIに合計を計算させない。

## 14.2 答案上の得点欄を読み取るときの注意

実資料の答案には、**採点者が点数を書き込むための欄が最初から印字されている。**
これを読み取って再利用する場合、次を取り違えると誤った値を拾う
（[実資料の構造](./grading-material-structure.md) §4.4）。

| 版面上の表記                 | 実体                                                            |
| ---------------------------- | --------------------------------------------------------------- |
| 斜線入りの四角の `得点/満点` | **設問ごとの得点欄。** 分母はその設問の配点であって満点ではない |
| 「総得点」ラベル付きの欄     | **テスト全体の合計。** 最終ページ下部にある                     |
| 囲みの無い `得点/満点`       | **設問ごとの得点。** 縦書きの教科では囲みが無かった             |
| フッタの `（k／m）ページ`    | **ページ番号。** 得点ではない                                   |

**「四角で囲まれているか」で合計点を判定してはならない。**
囲みの有無は「得点かページ番号か」の手がかりにはなるが、
**設問得点と総得点は区別できない。** 総得点は「総得点」というラベルで識別する。
**ラベルが読めない場合は推測せず、人に確認させる**（§35-2）。

なお、答案PDFが課題全体の一部だけを抜き出したものである場合、
**その PDF に総得点欄が含まれていないことがある**（§4.5.1）。
その場合、答案からはテストの満点も合計も分からない。配点は採点基準PDF側を正とする。

## 14.3 出力に含める欄

実資料の答案には、点数欄のほかに次の欄が印字されていた。

- **講評欄** — 添削者による全体コメント
- **添削者名の欄**

**MVPでこれらを埋めるかは未決**（§33）。決定5は設問ごとの点数と合計までしか定めていない。

---

# 15. メインUI

Material Design 3を利用する。

基本レイアウト：

```text
┌─────────────────────────────────────────────┐
│ Top App Bar                                 │
├──────────┬───────────────────┬──────────────┤
│          │                   │              │
│ Navigation│                  │ Inspector    │
│ Rail      │    PDF Viewer    │              │
│          │                   │ AI採点結果   │
│ 問1       │                   │ 採点基準     │
│ 問2       │                   │ コメント     │
│ 問3       │                   │ Confidence   │
│          │                   │              │
├──────────┴───────────────────┴──────────────┤
│ 修正       却下             承認して次へ    │
└─────────────────────────────────────────────┘
```

---

# 16. 主な画面

## 16.1 ホーム

表示内容：

- テスト一覧
- 最近使用したテスト
- 新規テスト登録
- 添削途中答案

> Issue #68で「起動確認用の足場」から実用的な画面へ作り直した
> （`app/lib/features/home/`）。上から「次の一手」1件、直近テストごとの
> 進み具合（確認済みの進捗バーと0件でない状態の内訳）、入口行（テスト登録 /
> 答案取込 / テスト一覧）。要確認・レビュー待ちの答案からは添削レビュー画面へ、
> `draft`のテストからはテスト設定画面へ直接戻れる。サイドカーの生死表示
> （`backend: ok`）はIssue #24のスプラッシュ/エラー画面と二重になるため撤去した。
> 状態は`GET /test-registrations`と`GET /tests/{id}/submissions`だけから
> 組み立てる。詳細・決定事項は[`home-dashboard.md`](./home-dashboard.md)。

---

## 16.2 テスト登録画面

入力：

- **採点基準PDF**（必須）
- **生徒答案PDF**（必須。回答欄の解析に使う。§6.2）
- 添削資料 Word / Excel（任意・強く推奨。**無い場合は警告を出し、精度が落ちることを伝える**）
- 添削サンプルPDF（任意）

処理：

- 形式非依存の下ごしらえ → 役割判定 → 構造抽出（§4.3.2）
- テストプロファイル生成（draft）

**この画面は「模範解答PDF」を要求しない**（決定1、§4.2）。

> **Issue #95 で入力を変更した。** 旧版は模範解答PDFと採点マニュアルPDFを
> 必須入力にしていた。既存の実装（`app/lib/features/test_registration/`）は
> 旧入力のままであり、本章に合わせた作り直しが要る。

> Issue #16でFlutter側の画面（`app/lib/features/test_registration/`）とAPI
> （`POST /tests`ほか）を実装した。登録直後はテストプロファイルも設問依存関係グラフも
> 未確認の`draft`状態で、両方を確認・確定するまでテスト登録は完了しない（§6.1）。
> 詳細は[`test-registration.md`](./test-registration.md)。

---

## 16.3 テスト設定画面

確認・修正：

- ページ
- 設問（**1設問が複数ページに跨る場合を含む**。§4.5.1）
- 回答欄
- **書字方向**（横書き / 縦書き）
- 配点
- 採点基準
- Annotation領域
- 設問依存関係グラフ（Issue #26のedge候補・並列実行層。確認するまで登録完了にならない）

**抽出できなかった項目は空欄で提示し、人が手入力する**（§4.4・決定8）。
AIが抽出した値と人が確定した値は区別して表示する。

> Issue #16で実装。§13の「PDF + Annotation Overlay」表示ではなく、region一覧を
> フィールド編集するUIとした（PDFオーバーレイでの視覚編集は未実装、後続Issueで検討）。
> 詳細・理由は[`test-registration.md`](./test-registration.md)。

---

## 16.4 答案取込画面

生徒答案PDFを追加する。

> Issue #17でFlutter側の画面（`app/lib/features/answer_intake/`）とAPI
> （`POST /tests/{test_id}/submissions`ほか）を実装した。取込方式は
> business-rules-and-evaluation-data.md §2 (4) の確定（1PDF = 1生徒、生徒識別は
> 人間が手動割当）に従う。詳細は
> [`answer-intake-and-preprocessing.md`](./answer-intake-and-preprocessing.md) §9・§11。

### 取り込み方法

取り込み方法を選択できるようにする

- 1PDF = 1生徒か
- 1PDFに複数生徒が含まれるか
- PDFファイル名に生徒識別情報が含まれるか
- 一括投入方法

### QRコードは使わない（決定6）

実資料の答案にはQRコードが印字されているが、**アプリはこれを読まない。**
様式・回・生徒の識別に使わない。

**理由**（次に検討する人が同じ道を通らないよう残す）:

1. **必ずあるとは限らない。** 目視した3教科で、QRが0個の答案・1個の答案・2個の答案が
   それぞれ存在した（[実資料の構造](./grading-material-structure.md) §4.1）
2. **中身が何かを確認していない。** 何の識別子が入っているか不明であり、
   塾の内部管理用である可能性が高い。意味を推測して使うことはできない
3. **他塾の資料にあるとは考えられない。** これは1つの塾の様式である
4. 生徒の識別は**人間による手動割当**で確定する方針が既にある（業務ルール §2 (4)）。
   QRで置き換える必要がない

再検討するなら、まず1と2（何個あるか・何が入っているか）をオーナーに確認すること。

---

## 16.5 添削レビュー画面

> Issue #21でFlutter側の画面（`app/lib/features/pdf_review/`）と、それを
> 支える読み取り専用API（`backend/src/auto_scoring/api/review_router.py`）を
> 実装した。Material 3のNavigation Rail + `pdfrx` PDF viewer +
> Widget Overlay（0〜1正規化座標）+ Inspector + action barで構成する。
> 承認・修正・却下はこの画面のメモリ内でのみ保持し、永続化と最終PDF生成は
> 後続Issueで扱う。詳細・決定事項は
> [`pdf-review-overlay.md`](./pdf-review-overlay.md)。

中央：

- PDF

右：

- AI認識文字
- 点数
- 採点根拠
- Confidence
- コメント
- 採点基準

操作：

- 承認
- 修正
- 却下
- AI再判定
- Annotation移動
- Annotation追加
- Annotation削除

> Issue #23でAppBarへ「PDF出力」ボタンを追加した
> （`app/lib/features/pdf_review/export_dialog.dart`）。未確認設問がある
> 場合の一覧表示、出力の進捗、保存先表示、失敗時の再試行を1つのdialogで
> 行う。詳細は[`pdf-export.md`](./pdf-export.md)。

---

# 17. キーボード操作

大量答案を処理するため、マウス操作だけに依存しない。

候補：

```text
Enter
→ 承認して次へ

E
→ 編集

X
→ AI結果却下

↑ ↓
→ 設問移動

Ctrl + Z
→ Undo
```

---

# 18. データ管理

## 18.1 DB

SQLiteを基本とする。

保持する主なEntity：

```text
Test

Question

Rubric

Submission

RecognitionResult

GradeResult

Annotation

Review
```

> 複数設問の依存関係（Issue #26）は上記に加えて `DependencyGraph` /
> `DependencyEdge` をテスト単位・バージョン管理で保持する。draft（AI候補）→
> 人間確認→confirmed の流れと、confirmed graphのみがSubmission処理を許可する
> ゲートは [`dependency-graph.md`](./dependency-graph.md) を参照。

---

# 19. Review履歴

AI結果と人間の最終判断は両方保存する。

例：

```text
AI
4 / 5点

↓

Human
3 / 5点
```

として記録する。

目的：

- 修正内容の確認
- AI精度評価
- 将来的な改善
- Undo
- 操作履歴

---

# 20. AI Provider

AIサービスをアプリ本体から分離する。

```text
AIProvider
 ├─ OpenAIProvider
 ├─ GeminiProvider
 ├─ ClaudeProvider
 └─ LocalProvider
```

---

# 21. バックエンド

暫定：

```text
Python
+
FastAPI
```

担当：

- PDF解析
- PDF生成
- OCR
- AI通信
- OpenCV画像処理
- テスト構造解析

Flutterとlocalhost通信する。

---

# 22. PDF処理

Python側で `pypdfium2`（ラスタライズ）と `pypdf`（オーバーレイ・出力）を利用する
（PoC 3 / Issue #12 で確定。旧記載は PyMuPDF）。

用途：

- PDF読込
- ページ画像化
- 座標取得
- Annotation描画
- PDF出力

---

# 23. ファイル構成

概念上は以下のように管理する。

```text
app-data/

├─ database.sqlite

├─ tests/
│   └─ <test-id>/
│       ├─ model-answer.pdf
│       ├─ manual.pdf
│       └─ profile.json
│
├─ submissions/
│   └─ <submission-id>/
│       └─ source.pdf
│
└─ exports/
    └─ corrected.pdf
```

実際の保存方式は実装時に決定する。

---

# 24. エラー処理

以下を正常系とは別に扱う。

### 資料の役割判定・構造抽出失敗（§4.3.2 段階2-3）

**人が全項目を手入力できる画面へ退避する**（決定8、§4.4）。
一部だけ取れた場合は、取れた項目を初期値として提示し、残りを人が埋める。
**推測値で埋めない。** 登録作業が止まらないことを優先する。

### PDF解析失敗

人間に通知する。**Word / Excel の読み取り失敗も同様に扱う。**

### 回答欄検出失敗

手動領域指定へ切り替える。

### OCR失敗

答案画像をそのまま人間へ提示する。

### AI API失敗

再実行可能にする。

### AI低Confidence

自動確定せず要確認状態にする。

### Annotation位置特定失敗

設問コメント領域へ退避する。

### PDF出力失敗

元PDFを破壊しない。

---

# 25. 状態管理

答案・設問は最低限以下の状態を持つ。

```text
未処理

AI処理中

AI処理済み

要確認

確認済み

出力済み

エラー
```

---

# 26. セキュリティ・個人情報

## 26.1 provider への送信（決定7）

**採点基準・添削資料・添削サンプル・生徒答案は、採用providerへ送ってよい。**
形式非依存の取り込み方針（§4.3）が成立するための前提である。

> **Issue #95 でこの制約は緩和された。** それ以前は「実データを外部APIに送らない」を
> 厳守していた。

**ただし「providerへ送ってよい」は「公開してよい」ではない。**
この区別を取り違えないこと。

|                                                                                 | 可否     |
| ------------------------------------------------------------------------------- | -------- |
| 採用providerへ送信する                                                          | **可**   |
| commit / PR / Issue / docs / CIログ / スクリーンショット / テストfixture に出す | **禁止** |

公開物のスクリーンショットには**合成フィクスチャだけ**を使う。
実データを読み込んだ画面を撮って貼ると公開される。詳細は業務ルール §2 (2)・§6.7。

## 26.2 付随して守ること

- **送信内容と応答をログに残さない。** エージェント・アプリのログ、記録データセット、
  例外メッセージのいずれにも残さない
- **何を送るかを利用者に示す。** 塾の資料と生徒の答案を扱う以上、黙って送らない
- **providerごとにデータの扱いが違う。** フォールバック連鎖に組む前に、各providerの
  データ保持・学習利用の設定を確認して記録する（業務ルール §2 (2)）
- 元答案をクラウドストレージへ自動アップロードしない
- ローカル保存を基本とする
- 不要な外部通信を行わない

---

# 27. データ保持

以下を決定する必要がある。

- 元答案の保存期間
- 添削済み答案の保存期間
- AIログの保存期間
- 生徒名等をDBへ保存するか
- アプリ終了後も答案を保持するか
- 一括削除機能が必要か

---

# 28. ログ

最低限以下を記録する。

- AI処理成功 / 失敗
- OCR失敗
- PDF解析失敗
- AI採点結果
- 人間による修正
- PDF出力結果

ただし、生徒答案本文をデバッグログへ無条件に書き込まない。

---

# 29. パフォーマンス

大量答案を想定し、

```text
答案1 → AI処理
答案2 → AI処理
答案3 → AI処理
...
```

をバックグラウンドキューとして処理できる構造にする。

人間が答案1をレビューしている間に、

```text
答案2
答案3
答案4
```

のAI処理を進められる構造を目標とする。

---

# 30. PoC

本格実装前に以下を検証する。

## PoC 1

手書き認識。

実際の答案で、

- 綺麗な字
- 普通の字
- 汚い字

をどの程度認識できるか確認する。

---

## PoC 2

採点。

実際の、

- 模範解答
- マニュアル
- 答案

を利用して、人間の採点との一致率を測定する。

---

## PoC 3

PDF座標。

PDF上の特定位置へ、

- ○
- ×
- コメント

を正しく表示・出力できるか確認する。

---

## PoC 4

レイアウト変更。

異なる形式のテストを複数投入し、

```text
テストプロファイル生成
↓
人間による修正
↓
各答案への適用
```

> PoC 4（Issue #15）で「生成 → 人間確認 → 再適用」の往復が成立することを検証した。
> プロファイルは常に未確認（DRAFT）で保存され、人間確認（`Profile.confirm`）を経て
> はじめて答案へ再適用できる。詳細・fixture・実測値は
> [`poc-4-multi-layout-profiles.md`](./poc-4-multi-layout-profiles.md)。

が機能するか確認する。

---

# 31. 最初のMVP

最初のバージョンでは以下に限定する。

### 実装する

- テスト登録
- **採点基準PDF読込**
- **添削資料（Word / Excel）読込**（任意入力）
- 生徒答案PDF読込
- **資料の役割判定・構造抽出（LLM）と、人による確認**（§4.3）
- **抽出失敗時の全項目手入力**（§4.4）
- **答案からの回答欄解析**（§6.2）
- PDF表示
- AI文字認識
- AI採点
- 点数表示（**設問ごと ＋ 合計**。§14.1）
- ○×表示
- コメント表示
- AI結果修正
- AI結果承認
- 添削済みPDF出力

### 実装しない（Issue #95 で範囲外と確認したもの）

- **模範解答PDFの読込**（決定1で必須入力から外した）
- **QRコードの読み取り**（決定6。§16.4）
- **矢印・手描き図のAnnotation**（決定4。§11.2・§11.3）

### 後回し候補

- AIの自動学習
- クラウド同期
- 複数端末同期
- チーム共有
- 詳細な分析ダッシュボード
- 完全自動添削
- 複数AIによるConsensus採点

---

# 32. 技術スタック

> 本章の暫定項目・抽象化（`AIProvider` / `OCRProvider` 等）の最終決定は
> `[technology-stack.md](./technology-stack.md)`（技術スタック調査・決定書）にまとめた。
> 食い違う場合はそちらを優先する。

## Desktop / UI

```text
Flutter
Dart
Material Design 3
```

## Backend

```text
Python
FastAPI
Pydantic
```

## PDF

```text
pypdfium2 + pypdf
```

## Image Processing

```text
OpenCV
```

## Database

```text
SQLite
```

## AI

```text
AIProvider abstraction
```

具体的なモデルは要判断。

## OCR

```text
OCRProvider abstraction
```

具体的なサービスはPoC後に判断する。

---

# 33. 未決定事項

実装前またはPoC後にユーザー判断が必要な項目。
各項目の現時点の反映状況は `[technology-stack.md](./technology-stack.md)` §7 を参照。

業務ルール・対象範囲・データ保持（本章の項目 4〜18、20〜23 および §27・§31）の確定は
`[business-rules-and-evaluation-data.md](./business-rules-and-evaluation-data.md)`（GitHub
Issue #8）で行った。食い違う場合はそちらを優先する。

## 33.1 Issue #95 で解決した項目

| 本章の項目                      | 決定                                                                        |
| ------------------------------- | --------------------------------------------------------------------------- |
| 4. 外部AI APIへ答案を送信可能か | **可**。採点資料・生徒答案とも送ってよい。公開は禁止（§26.1）               |
| 5. 答案PDFの実際のファイル構造  | 実資料で確認済み（§4.5.1、[実資料の構造](./grading-material-structure.md)） |
| 7. 必要な添削記号               | ボックス・下線・テキスト。図形は将来（§11）                                 |
| 9. 点数の記入位置               | 設問ごと ＋ 合計（§14.1）。答案上の得点欄の見分けは §14.2                   |
| 10. 採点方式                    | 加点式・減点式の両方。設問ごとに異なりうる（§9.1）                          |

## 33.2 Issue #95 で新たに生じた未決事項

| 項目                                                   | 状況                                                                    |
| ------------------------------------------------------ | ----------------------------------------------------------------------- |
| 講評欄・添削者名欄を出力PDFに埋めるか                  | **未決**（§14.3）。決定5は設問ごとの点数と合計までしか定めていない      |
| インラインの加減点表示（対象箇所ごとの `(+3)` `(-1)`） | **未決**（§11.4）                                                       |
| 下線の線種（直線・波線・破線）を作り分けるか           | **未決**（§11.4）                                                       |
| 縦書き答案のAnnotation配置と座標モデル                 | **未決。** 縦書きが実在することは確認済み（§4.5.1）だが、実装方針は未定 |
| 数式・英作文・図グラフを対象外にしたままでよいか       | **要再検討。** 実資料の教科構成と噛み合っていない（業務ルール §5）      |
| テスト登録時に答案が必要になることの運用影響           | **未決**（§6.2 の制約）                                                 |
| providerごとのデータ保持・学習利用の設定               | **未確認。** フォールバック連鎖に組む前に確認して記録する（§26.2）      |

1. Windows専用とするか
2. 使用するAIモデル
3. 使用するOCR
4. 外部AI APIへ答案を送信可能か
5. 答案PDFの実際のファイル構造
6. 1PDFあたりの生徒数
7. 必要な添削記号
8. コメントの記入ルール
9. 点数の記入位置
10. 採点方式

- 加点式
  - 減点式
  - 両方

1. 部分点の扱い
2. 記述式以外の問題を対象とするか
3. 数式を対象とするか
4. 英作文を対象とするか
5. 図・グラフ問題を対象とするか
6. 答案保存期間
7. AI処理履歴保存期間
8. 生徒識別情報を保存するか
9. AI低Confidenceの基準値
10. PDF出力形式
11. 元PDFへ上書きするか

- 原則として新規PDF出力を推奨

1. キーボードショートカット
2. AIによる自動コメント生成をどこまで許可するか
3. 複数答案の並列AI処理数

---

# 34. 全体処理フロー

```text
【テスト登録】

採点基準PDF ＋ 生徒答案PDF（必須）
＋ 添削資料 Word/Excel ＋ 添削サンプル（任意）
        ↓
形式非依存の下ごしらえ（PDF: 抽出+画像化 / Word・Excel: テキストと表）
        ↓
役割の判定・構造の抽出（LLM）
        ↓
テストプロファイル（draft）
        ↓
人間が確認 ←→ 取れなかった項目は手入力
        ↓
登録（confirmed）


【答案処理】

生徒答案PDF
        ↓
画像前処理
        ↓
レイアウト位置合わせ
        ↓
回答欄抽出
        ↓
OCR + Vision
        ↓
文字認識
        ↓
採点AI
        ↓
点数 / コメント / Annotation
        ↓
PDF Overlay
        ↓
人間レビュー
        ↓
承認 / 修正 / 却下
        ↓
PDF書き込み
        ↓
添削済みPDF
```

---

# 35. 設計上の最重要原則

本システムでは以下を守る。

1. AIの結果を無条件で確定しない
2. 読めない文字を無理に推測しない
3. AIにPDF座標を直接決めさせない
4. 元PDFを破壊しない
5. AI結果と人間の最終結果を分離して保持する
6. OCR・AIサービスを交換可能にする
7. テスト形式が変わることを前提とする。**手元の実資料は1つの塾の1期分でしかない**
8. 人間の確認作業自体を高速化する
9. 生徒データの外部送信可否を最優先で確認する
10. 実際の答案を用いたPoCで精度を確認してから本格開発する
11. **形式ごとのparserを書かない。文書をLLMに理解させ、人が確認する**（§4.3）
12. **抽出できなかったものは「取れなかった」と表示する。推測で埋めない**（§4.4）
13. **仕様を書く前に実物を見る。** 実資料を見ずに入力仕様を決めた結果、
    存在しない入力（模範解答PDF）を必須にした画面を作り込んだ（Issue #95）
