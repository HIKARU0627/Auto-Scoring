# Agent skill trees are managed as-is (external skills + generated mirror)
.agents/skills/
.claude/skills/
skills-lock.json
pnpm-lock.yaml

# Flutter app and Python backend own their formatters (dart format / ruff format)
app/
backend/
